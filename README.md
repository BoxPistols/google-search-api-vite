# 🔍 Google Search Ranking Checker

Google Custom Search APIを使用して、検索キーワードの順位をチェックするSEOツールです。

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.7-blue)](https://www.typescriptlang.org/)
[![React](https://img.shields.io/badge/React-18.2-61dafb)](https://reactjs.org/)
[![Vite](https://img.shields.io/badge/Vite-5.4-646cff)](https://vitejs.dev/)

## ✨ 特徴

- **検索順位チェック**: 1-20位までの検索結果を取得・表示
- **クエリ消費量の可視化**: リアルタイムで消費クエリ数を計算・表示
- **3段階アクセス制限**:
  - 未ログイン: 10クエリ/日
  - ログイン済み: 50クエリ/日
  - オーナー: 500クエリ/日
- **Google認証**: Firebase Authenticationによる安全なログイン
- **ダークモード**: 目に優しいダークテーマ対応
- **レスポンシブデザイン**: PC・タブレット・スマートフォン完全対応
- **SVGアイコン**: Material-UI Iconsで統一された美しいUI

## 📋 目次

- [🎯 エンドユーザー向けガイド](#-エンドユーザー向けガイド)
- [👨‍💻 開発者向けガイド](#-開発者向けガイド)
- [❓ FAQ](#-faq)
- [🔧 トラブルシューティング](#-トラブルシューティング)

---

## 🎯 エンドユーザー向けガイド

### 基本的な使い方

#### ステップ1: 検索キーワードを入力

```
例: React Next.js TypeScript
```

- スペース区切りで複数キーワードを入力できます
- 入力すると自動的に消費クエリ数が計算されます

#### ステップ2: 消費クエリを確認

画面に表示される情報：

| 項目 | 説明 | 例 |
|-----|------|-----|
| キーワード数 | 入力したキーワードの数 | 3個 |
| 消費予定 | この検索で消費するクエリ数 | 6クエリ |
| 残り | 本日の残りクエリ数 | 44クエリ |

**計算式**: `消費クエリ = 2ページ × キーワード数`

#### ステップ3: 検索実行

- ✅ クォータが十分な場合: 緑色の「検索 (-6)」ボタン
- ❌ クォータ不足の場合: 赤色の「クォータ不足」ボタン（無効）

#### ステップ4: 結果の確認

検索結果に表示される情報：
- **順位**: 検索結果の順位（1-20位）
- **タイトル**: ページタイトル
- **URL**: ページのURL
- **説明文**: 検索結果のスニペット

**追加機能**:
- **ドメイン分析**: 上位に表示されているドメインの統計
- **検索履歴**: 過去の検索結果を再表示
- **検索統計**: 総検索回数、総クエリ消費数

### ログイン方法

#### 🔓 未ログイン（ゲスト）

- **クォータ**: 10クエリ/日
- **メリット**: アカウント不要ですぐに使える
- **制限**: 少ないクエリ数

#### 🔐 Googleでログイン

1. 右上の「Googleでログイン」ボタンをクリック
2. Googleアカウントを選択
3. 権限を許可

**ログイン後**:
- **クォータ**: 50クエリ/日に拡張
- **アバター表示**: ユーザー名と写真が表示されます
- **メニュー**: アバターをクリックでログアウト可能

#### 👑 オーナーアカウント

- 設定されたオーナーメールアドレスでログインすると自動認識
- **クォータ**: 500クエリ/日
- **バッジ**: 「オーナー」と表示されます
- **用途**: アプリ管理者用

### クォータ制限について

#### 制限一覧

| ユーザータイプ | 1日の制限 | ログイン | 用途 |
|--------------|----------|---------|------|
| 🔓 ゲスト | 10クエリ | 不要 | お試し利用 |
| 🔐 一般ユーザー | 50クエリ | 必要 | 通常利用 |
| 👑 オーナー | 500クエリ | 必要 | 管理者 |

#### クエリ消費の例

| 検索キーワード | キーワード数 | 消費クエリ | 計算 |
|--------------|------------|----------|------|
| `React` | 1 | 2 | 2ページ × 1 |
| `React Next.js` | 2 | 4 | 2ページ × 2 |
| `React Next.js TypeScript` | 3 | 6 | 2ページ × 3 |

#### リセット時刻

- **毎日0:00**（ローカル時刻）に自動リセット
- リセットまでの残り時間が表示されます

#### 警告システム

- 🟡 **警告**: 残りクエリが少なくなると黄色で表示
- 🔴 **エラー**: クォータ不足時は赤色で表示され、検索不可

### その他の機能

#### 🌙 ダークモード

- 右上のアイコンをクリックして切り替え
- 設定はブラウザに保存され、次回も保持されます

#### 📊 統計情報

- **総検索回数**: これまでの検索回数
- **総クエリ消費数**: 累計の消費クエリ数
- **最新の検索**: 最後に検索したキーワード

#### 📋 検索履歴

- 最大50件まで保存
- クリックで過去の結果を再表示
- 削除ボタンで履歴をクリア可能

---

## 👨‍💻 開発者向けガイド

### 🚀 クイックスタート

```bash
# リポジトリをクローン
git clone https://github.com/BoxPistols/google-search-api-vite.git
cd google-search-api-vite

# 依存関係をインストール
pnpm install

# 環境変数を設定
cp .env.example .env
# .envファイルを編集

# 開発サーバーを起動
pnpm dev
```

ブラウザで http://localhost:5173 を開く

### 📋 必要な環境

| 項目 | バージョン | 必須 |
|-----|-----------|------|
| Node.js | 18.x以上 | ✅ |
| pnpm | 8.x以上 | ✅ |
| Google Cloud アカウント | - | ✅ |
| Firebaseアカウント | - | ⚪️（認証機能用） |

### 🔑 環境変数の設定

#### 最小構成（認証なし）

Google Custom Search APIのみで動作します：

```bash
# .env
VITE_GOOGLE_API_KEY=your_google_api_key_here
VITE_GOOGLE_SEARCH_ID=your_search_engine_id_here
```

#### 完全版（認証あり）

Firebase認証を追加する場合：

```bash
# .env
# Google Custom Search API（必須）
VITE_GOOGLE_API_KEY=your_google_api_key_here
VITE_GOOGLE_SEARCH_ID=your_search_engine_id_here

# Firebase設定（オプション）
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# オーナーメールアドレス
VITE_OWNER_EMAIL=your-email@gmail.com
```

### 🔧 Google Custom Search API設定

#### ステップ1: Google Cloud Console

1. https://console.cloud.google.com/ にアクセス
2. 新しいプロジェクトを作成
3. 「APIとサービス」→「ライブラリ」
4. 「Custom Search API」を検索して有効化

#### ステップ2: APIキー取得

1. 「APIとサービス」→「認証情報」
2. 「認証情報を作成」→「APIキー」
3. APIキーをコピーして`.env`に設定

#### ステップ3: 検索エンジンID取得

1. https://programmablesearchengine.google.com/ にアクセス
2. 「新しい検索エンジン」を作成
3. **検索対象**: 「ウェブ全体を検索」を選択
4. 検索エンジンIDをコピーして`.env`に設定

### 🔥 Firebase設定（オプション）

認証機能を使用する場合のみ必要です。

詳細は [FIREBASE_SETUP.md](./FIREBASE_SETUP.md) を参照してください。

#### 簡易手順

1. **Firebaseプロジェクト作成**
   ```
   https://console.firebase.google.com/
   → 「プロジェクトを追加」
   ```

2. **Authentication設定**
   ```
   Authentication → 始める → Google を有効化
   ```

3. **Webアプリ登録**
   ```
   プロジェクト設定 → アプリを追加 → Web
   → 設定情報を.envに追加
   ```

4. **承認済みドメイン**
   ```
   Authentication → Settings → 承認済みドメイン
   → localhost（開発用）
   → your-app.vercel.app（本番用）
   ```

### 📦 開発コマンド

```bash
# 開発サーバー起動
pnpm dev

# ビルド
pnpm build

# プレビュー（ビルド後）
pnpm preview

# Lint
pnpm lint

# フォーマット（自動修正）
pnpm fix
```

### 🚢 デプロイ

#### Vercelへのデプロイ（推奨）

```bash
# Vercel CLIをインストール
npm i -g vercel

# ログイン
vercel login

# デプロイ
vercel
```

#### 環境変数の設定

Vercelダッシュボード:
1. プロジェクトの「Settings」→「Environment Variables」
2. `.env`の内容をすべて追加
3. Production、Preview、Development すべてにチェック

### 📁 プロジェクト構成

```
google-search-api-vite/
├── src/
│   ├── components/              # UIコンポーネント
│   │   ├── AuthButton.tsx       # 認証ボタン
│   │   ├── SearchForm.tsx       # 検索フォーム（クエリ計算）
│   │   ├── QuotaDisplay.tsx     # クォータ表示
│   │   ├── ResultsTable.tsx     # 検索結果テーブル
│   │   ├── SearchHistory.tsx    # 検索履歴
│   │   ├── SearchStats.tsx      # 統計情報
│   │   └── DomainAnalysis.tsx   # ドメイン分析
│   ├── contexts/                # Reactコンテキスト
│   │   └── AuthContext.tsx      # 認証状態管理
│   ├── services/                # 外部サービス
│   │   └── firebase.ts          # Firebase初期化
│   ├── utils/                   # ユーティリティ
│   │   ├── apiQuotaManager.ts   # クォータ管理
│   │   ├── userSettings.ts      # ユーザー設定
│   │   └── localStorage.ts      # ローカルストレージ
│   ├── types/                   # TypeScript型定義
│   │   ├── user.ts              # ユーザー型
│   │   └── search.ts            # 検索結果型
│   ├── util/                    # テーマ
│   │   └── theme.ts             # Material-UIテーマ
│   └── App.tsx                  # メインアプリ
├── public/                      # 静的ファイル
├── .env.example                 # 環境変数サンプル
├── FIREBASE_SETUP.md            # Firebase設定ガイド
├── README_AUTH.md               # 認証機能ガイド
├── BACKEND_DESIGN.md            # バックエンド設計書
├── TEST_CHECKLIST.md            # テストチェックリスト
└── package.json                 # 依存関係
```

### 🔌 主要な技術スタック

| カテゴリ | 技術 | 用途 |
|---------|------|------|
| フレームワーク | React 18 | UI構築 |
| 言語 | TypeScript 5.7 | 型安全な開発 |
| ビルドツール | Vite 5.4 | 高速ビルド |
| UIライブラリ | Material-UI 6 | デザインシステム |
| 認証 | Firebase Auth | Google認証 |
| アイコン | Material-UI Icons | SVGアイコン |
| スタイリング | Emotion | CSS-in-JS |
| API | Google Custom Search | 検索機能 |

### 🧪 テスト

テストチェックリスト: [TEST_CHECKLIST.md](./TEST_CHECKLIST.md)

```bash
# 開発サーバーでテスト
pnpm dev

# ビルドテスト
pnpm build
pnpm preview
```

---

## ❓ FAQ

### Q1. Firebase設定なしで使えますか？

**A.** はい、使えます。

- Firebase未設定の場合、ゲストユーザーとして10クエリ/日で利用可能
- 認証機能が無効化され、「認証未設定」ボタンが表示されます
- Google Custom Search APIのみで基本機能が動作します

### Q2. 複数のブラウザで同じアカウントを使えますか？

**A.** 現在はブラウザごとに管理されています。

- クォータはlocalStorageに保存されるため、ブラウザ間で共有されません
- Firestoreに移行すると、デバイス間で同期可能になります
- 詳細: [BACKEND_DESIGN.md](./BACKEND_DESIGN.md)

### Q3. クォータはいつリセットされますか？

**A.** 毎日0:00（ローカル時刻）に自動リセットされます。

- タイムゾーンはブラウザのローカル時刻に基づきます
- リセットまでの残り時間が画面に表示されます

### Q4. オーナーアカウントを複数設定できますか？

**A.** 現在は1つのメールアドレスのみ対応しています。

複数設定する場合の改修方法：
```typescript
// src/types/user.ts
export const OWNER_EMAILS = [
  'owner1@example.com',
  'owner2@example.com',
];

// src/contexts/AuthContext.tsx
const determineUserType = (firebaseUser: User | null): UserType => {
  if (!firebaseUser) return 'guest';
  if (OWNER_EMAILS.includes(firebaseUser.email || '')) return 'owner';
  return 'user';
};
```

### Q5. Google Custom Search APIの費用は？

**A.**
- **無料枠**: 100クエリ/日まで無料
- **有料**: $5/1000クエリ（最大10,000クエリ/日）

このアプリの設定：
- ゲスト: 10クエリ/日 → 無料枠内
- 一般ユーザー: 50クエリ/日 → 無料枠内
- オーナー: 500クエリ/日 → **有料プラン必要**

詳細: https://developers.google.com/custom-search/v1/overview

### Q6. Firebase Authenticationの費用は？

**A.**
- **無料枠**: 月10,000アクティブユーザーまで無料
- 個人利用や小規模なチーム（<100人）では無料枠で十分です

詳細: https://firebase.google.com/pricing

### Q7. 検索結果が0件になる

**A.** 以下を確認してください：

1. APIキーとSearch IDが正しいか
2. Google Custom Search APIが有効化されているか
3. 検索エンジンが「ウェブ全体を検索」に設定されているか
4. Google側のAPIクォータ（100クエリ/日）を超えていないか

### Q8. ダークモードが保存されない

**A.** ブラウザのlocalStorageが有効か確認してください：

```javascript
// ブラウザのコンソールで確認
localStorage.getItem('darkMode')
```

プライベートブラウジングモードでは保存されません。

---

## 🔧 トラブルシューティング

### ログインできない

**症状**: 「Googleでログイン」ボタンをクリックしても反応しない

**解決方法**:

1. **Firebase Consoleを確認**
   ```
   Authentication → Sign-in method → Google が有効か
   ```

2. **承認済みドメインを確認**
   ```
   Authentication → Settings → 承認済みドメイン
   → localhost が登録されているか
   ```

3. **ブラウザコンソールでエラー確認**
   ```
   F12 → Console タブ
   → エラーメッセージを確認
   ```

4. **環境変数を確認**
   ```bash
   # .envファイルの内容を確認
   cat .env
   # 開発サーバーを再起動
   pnpm dev
   ```

### オーナーとして認識されない

**症状**: ログインしても「一般ユーザー」と表示される

**解決方法**:

1. **環境変数を確認**
   ```bash
   # .envファイルの VITE_OWNER_EMAIL を確認
   grep VITE_OWNER_EMAIL .env
   ```

2. **メールアドレスを確認**
   - ログインしているGoogleアカウントのメールアドレス
   - `.env`の`VITE_OWNER_EMAIL`
   - **完全一致**が必要（大文字小文字も区別）

3. **開発サーバーを再起動**
   ```bash
   # 環境変数変更後は必須
   pnpm dev
   ```

4. **ブラウザのキャッシュをクリア**
   ```
   F12 → Application → Storage → Clear site data
   ```

### クエリが消費されない / 表示がおかしい

**症状**: 検索しても残りクエリが減らない

**解決方法**:

1. **localStorageをクリア**
   ```javascript
   // ブラウザのコンソールで実行
   localStorage.clear()
   location.reload()
   ```

2. **ブラウザのキャッシュをクリア**
   ```
   Ctrl+Shift+Delete → キャッシュをクリア
   ```

3. **コンソールでエラー確認**
   ```
   F12 → Console → エラーメッセージを確認
   ```

### 検索結果が0件

**症状**: 検索しても結果が表示されない

**解決方法**:

1. **APIキーとSearch IDを確認**
   ```bash
   # .envファイルを確認
   grep VITE_GOOGLE .env
   ```

2. **Google Cloud Consoleを確認**
   ```
   APIとサービス → 認証情報 → APIキーが有効か
   Custom Search API → 有効化されているか
   ```

3. **検索エンジン設定を確認**
   ```
   https://programmablesearchengine.google.com/
   → 「ウェブ全体を検索」が有効か
   ```

4. **ネットワークタブで確認**
   ```
   F12 → Network → XHR
   → googleapis.com へのリクエストを確認
   → レスポンスを確認
   ```

5. **APIクォータを確認**
   ```
   Google Cloud Console → APIs & Services → Dashboard
   → Custom Search API → クォータ
   → 100クエリ/日を超えていないか
   ```

### ビルドエラー

**症状**: `pnpm build`でエラーが発生

**解決方法**:

1. **依存関係を再インストール**
   ```bash
   rm -rf node_modules pnpm-lock.yaml
   pnpm install
   ```

2. **TypeScriptエラーを確認**
   ```bash
   # 型チェック
   pnpm tsc --noEmit
   ```

3. **環境変数を確認**
   ```bash
   # .envファイルの構文エラーを確認
   cat .env
   # 引用符が正しいか、改行があるか
   ```

4. **キャッシュをクリア**
   ```bash
   # Viteのキャッシュをクリア
   rm -rf node_modules/.vite
   pnpm build
   ```

### Firebase初期化エラー

**症状**: コンソールに「Firebase initialization failed」と表示

**解決方法**:

1. **環境変数をすべて設定**
   ```bash
   # .envに以下がすべて設定されているか確認
   VITE_FIREBASE_API_KEY
   VITE_FIREBASE_AUTH_DOMAIN
   VITE_FIREBASE_PROJECT_ID
   VITE_FIREBASE_STORAGE_BUCKET
   VITE_FIREBASE_MESSAGING_SENDER_ID
   VITE_FIREBASE_APP_ID
   ```

2. **Firebase Consoleで確認**
   ```
   プロジェクト設定 → マイアプリ → Firebase SDK snippet
   → 設定値が正しいか確認
   ```

3. **認証なしで使う**
   ```bash
   # Firebaseの環境変数をコメントアウトまたは削除
   # ゲストユーザーとして動作します
   ```

---

## 📄 ライセンス

MIT License - 自由に使用・改変・配布できます

## 🤝 コントリビューション

プルリクエストを歓迎します！

1. このリポジトリをフォーク
2. 新しいブランチを作成 (`git checkout -b feature/amazing-feature`)
3. 変更をコミット (`git commit -m 'Add amazing feature'`)
4. ブランチにプッシュ (`git push origin feature/amazing-feature`)
5. プルリクエストを作成

## 💬 サポート

問題が発生した場合：

- 🐛 [Issues](https://github.com/BoxPistols/google-search-api-vite/issues) でバグ報告
- 💡 [Discussions](https://github.com/BoxPistols/google-search-api-vite/discussions) で質問・提案

## 👨‍💻 開発者

- [BoxPistols](https://github.com/BoxPistols)

## 📚 関連ドキュメント

| ドキュメント | 内容 |
|------------|------|
| [README_AUTH.md](./README_AUTH.md) | 認証機能の詳細ガイド |
| [FIREBASE_SETUP.md](./FIREBASE_SETUP.md) | Firebase設定の完全ガイド |
| [BACKEND_DESIGN.md](./BACKEND_DESIGN.md) | クロスデバイス同期の設計 |
| [TEST_CHECKLIST.md](./TEST_CHECKLIST.md) | テストチェックリスト |

---

<p align="center">Made with ❤️ for SEO professionals</p>
